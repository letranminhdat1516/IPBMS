/// Constants used in the profile screen
class ProfileConstants {
  static const String defaultAvatarUrl =
      'https://via.placeholder.com/400x400/4A90E2/FFFFFF?text=User';

  // Animation durations
  static const Duration fadeDuration = Duration(milliseconds: 800);
  static const Duration scaleDuration = Duration(milliseconds: 600);
  static const Duration switchDuration = Duration(milliseconds: 300);
  static const Duration iconSwitchDuration = Duration(milliseconds: 200);

  // Avatar dimensions
  static const double avatarRadius = 56.0;
  static const double avatarBorderWidth = 6.0;
  static const double avatarInnerPadding = 4.0;

  // UI dimensions
  static const double cardBorderRadius = 18.0;
  static const double fieldBorderRadius = 12.0;
  static const double buttonBorderRadius = 14.0;
  static const double dialogBorderRadius = 16.0;
  static const double snackBarBorderRadius = 10.0;

  // Spacing
  static const double horizontalPadding = 20.0;
  static const double verticalPadding = 32.0;
  static const double fieldSpacing = 12.0;
  static const double sectionSpacing = 24.0;

  // Icon sizes
  static const double infoIconSize = 20.0;
  static const double editIconSize = 24.0;
  static const double checkIconSize = 18.0;
  static const double avatarIconSize = 40.0;

  // Shadow values
  // Shadow values
  static const double avatarShadowBlur = 8.0;
  static const double fieldShadowBlur = 2.0;
  static const double infoShadowBlur = 1.5;

  // Loading simulation duration
  static const Duration saveDelay = Duration(seconds: 2);
}
